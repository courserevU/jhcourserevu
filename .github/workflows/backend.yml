
name: Deploy the backend

on:
  pull_request:
    branches:
      - main
      - iteration-1
      - iteration-2
      - iteration-3
      - iteration-4
      - iteration-5
      - iteration-6
      # TODO: add temp branches here
  
  push:
    branches:
      - main
      - iteration-1
      - iteration-2
      - iteration-3
      - iteration-4
      - iteration-5
      - iteration-6
      # TODO: add temp branches here

    paths:
      - "code/backend/**"
      - ".github/workflows/backend.yml"

defaults:
  run:
    working-directory: code/backend

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      # Add steps to build and deploy the backend